import Foundation

// MARK: - CostRecord Legacy Compatibility
/// `CostRecord` is an alias for `CostEvent`, representing financial expenses allocated to protocols and inventory.
public typealias CostRecord = CostEvent
