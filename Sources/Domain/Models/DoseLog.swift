import Foundation

// MARK: - DoseLog Legacy Compatibility
/// `DoseLog` is an alias for `DoseEvent`, representing the ground-truth actual execution of a dose.
public typealias DoseLog = DoseEvent
public typealias DoseStatus = DoseEventStatus
