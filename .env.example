# Server Configuration
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=development

# PostgreSQL Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=vialr
# Alternatively, full connection string:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/vialr

# Security & JWT
JWT_SECRET=your_super_secret_jwt_signing_key_must_be_at_least_32_characters_long
